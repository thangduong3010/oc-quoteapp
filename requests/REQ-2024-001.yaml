- name: pltf-web-001
    os:
      image: quay.io/containerdisks/windows:latest  # ← not allowed
    compute:
      cpu: 8                                         # ← exceeds limit
      memory: 32Gi                                   # ← exceeds limit
    disk:
      size: 100Gi                                    # ← exceeds limit
      storageClass: fake-sc                          # ← not allowed
      accessMode: ReadWriteOnce
    network:
      interfaces:
        - name: default
          network: vlan-100-web                      # ← not allowed
